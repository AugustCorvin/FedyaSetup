# 👋 Привет!

> **Данный репозиторий является ЛИЧНЫМ сборником команд для первоначальной настройки Fedora. Если ты каким-то образом нашел данный репозиторий, то тоже можешь изучить и использовать его для настройки своей системы :)**

> **За основу был взят [Fedora-Noble-Setup](https://github.com/wz790/Fedora-Noble-Setup)**


---


# 📋 Навигация

### 1. [Обновление системы и пакетов](#1-обновление-системы-и-пакетов-1)
  - [Установка RPM Fusion](#установка-rpm-fusion)
  - [Обновление всех пакетов](#обновление-всех-пакетов)
  - [Обновление прошивки](#обновление-прошивки)
  - [Даем компьютеру имя](#даем-компьютеру-имя)

### 2. [Установка дополнительного ПО](#2-установка-дополнительного-по)
  - [Flathub Setup](#flathub-setup)

### 3. [Драйвера AMD & Intel](#3-драйвера-amd--intel)
  - [Для AMD и Intel](#flathub-setup)
  - [Только для AMD](#только-для-amd)
  - [Intel (Новые GPUs)](#intel-новые-gpus)
  - [Intel (Старые GPUs)](#intel-старые-gpus)


### 4. [Делаем медия рабочими](#4-делаем-медия-рабочими)
  - [Видеокодеки](#видеокодеки)
  - [Исправляем проблему с кодеком H.264 в Firefox](#исправляем-проблему-с-кодеком-h264-в-firefox)

### 5. [Полезные дополнения](#3-драйвера-amd--intel)
  - [Поддержка архивов .rar](#5-полезные-дополнения)
  - [Шрифты Microsoft](#поддержка-архивов-rar)
  - [Поддержка AppImage](#поддержка-appimage)
  - [Более быстрая загрузка системы](#более-быстрая-загрузка-системы)

### 6. [Поддерживаем систему в чистоте](#6-поддерживаем-систему-в-чистоте)


---


# 1. Обновление системы и пакетов

### Установка RPM Fusion

> **RPM Fusion — это community-поддерживаемый репозиторий, который добавляет пакеты, которые Fedora и Red Hat не могут распространять из-за лицензий и юридических ограничений. Сюда обычно входят: мультимедийные кодеки, проприетарные драйвера (NVIDIA), закрытое или «юридически сложное» ПО**

```bash
# Получаем открытые репозитории

sudo dnf install -y \
  https://mirrors.rpmfusion.org/free/fedora/rpmfusion-free-release-$(rpm -E %fedora).noarch.rpm
```

```bash
# Получаем закрытые репозитории

sudo dnf install -y \
  https://mirrors.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm
```

```bash
# Обновляем все

sudo dnf group upgrade core -y
```

### Обновление всех пакетов

> **Переодически необходимо обновлять все пакеты**

```bash
sudo dnf update -y
```

### Обновление прошивки

```bash
# Смотрим, что можно обновить

sudo fwupdmgr get-devices
```

```bash
# Обновляем базу данных прошивки

sudo fwupdmgr refresh --force
```

```bash
# Проверяем наличие обновлений

sudo fwupdmgr get-updates
```

```bash
# Применяем их

sudo fwupdmgr update
```

```bash
# Перезагружаемся

sudo reboot
```

### Даем компьютеру имя

> **Можно просто скопировать команду и оставить имя fedora, либо можно придумать что-то свое**

```bash
sudo hostnamectl set-hostname fedora
```


---


# 2. Установка дополнительного ПО

## Flathub Setup

> **Fedora по умолчанию поставляется с урезанной версией Flatpak, нужно исправить это >:)**

```bash
# Удалим урезанную версию

flatpak remote-delete fedora
```

```bash
# Установим все репозитории

flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
```


--- 


# 3. Драйвера AMD & Intel

### Для AMD и Intel:

```bash
# Базовые драйвера и  поддержка Vulkan

sudo dnf install -y mesa-dri-drivers mesa-vulkan-drivers vulkan-loader mesa-libGLU
```

### Только для AMD:

```bash
# Аппаратное ускорение видео (делает воспроизведение видео плавнее)

sudo dnf install -y mesa-va-drivers-freeworld mesa-vdpau-drivers-freeworld
```

### Intel (Новые GPUs):

```bash
# Аппаратное ускорение видео
sudo dnf install -y intel-media-driver
```

### Intel (Старые GPUs):

```bash
# Аппаратное ускорение видео

sudo dnf install -y libva-intel-driver
```


---


## 4. Делаем медия рабочими

> **Fedora по умолчанию поставляется практически без кодеков, из-за чего большинство меня просто не будет работать,  нужно исправить это >:)**

### Видеокодеки

```bash
# Заменяем урезанный ffmpeg на полноценный

sudo dnf swap -y ffmpeg-free ffmpeg --allowerasing
```

```bash
# Устанавливаем все плагины GStreamer

sudo dnf install -y gstreamer1-plugins-{bad-\*,good-\*,base} \
  gstreamer1-plugin-openh264 gstreamer1-libav lame\* \
  --exclude=gstreamer1-plugins-bad-free-devel
```

```bash
# Устанавливаем мультимедийные группы пакетов

sudo dnf group install -y multimedia && sudo dnf group install -y sound-and-video
```


---


### Исправляем проблему с кодеком H.264 в Firefox

```bash
# Устанавливаем кодек Cisco

sudo dnf install -y openh264 gstreamer1-plugin-openh264 mozilla-openh264
```

```bash
# Включаем репозиторий Cisco

sudo dnf config-manager --set-enabled fedora-cisco-openh264 && sudo dnf update -y
```


---


# 5. Полезные дополнения

### Поддержка архивов .rar

```bash
sudo dnf install -y p7zip p7zip-plugins unrar
```

### Шрифты Microsoft

```bash
# Устанавливаем зависимости

sudo dnf install -y curl cabextract xorg-x11-font-utils fontconfig
```

```bash
# Уставливаем шрифты

sudo rpm -i --nodigest --nosignature https://downloads.sourceforge.net/project/mscorefonts2/rpms/msttcore-fonts-installer-2.6-1.noarch.rpm
```

```bash
# Обновляем кэш шрифтов

sudo fc-cache -fv
```

### Поддержка AppImage

```bash
# Устанавливаем FUSE

sudo dnf install -y fuse fuse-libs
```

```bash
# Необязательно: менеджер AppImage

flatpak install -y flathub it.mijorus.gearlever
```

### Более быстрая загрузка системы

```bash
sudo systemctl disable NetworkManager-wait-online.service
```


---


# 6. Поддерживаем систему в чистоте

```bash
# Очистить кэш пакетов
sudo dnf clean all
```

```bash
# Удалить осиротевшие пакеты

sudo dnf autoremove -y
```


---
